
	pid_t pid = fork();
	
	if(pid != 0){ 
		pid_t child_id = wait(NULL);
		
		if(child_id == -1){
			err(1, "error in waiting");
		}
	}
	

	
	if(pid == 0){
		execlp(command_path, argv[1], (char*)NULL);
	}
	
	exit(0);
